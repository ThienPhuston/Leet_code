#include <iostream>
using namespace std;
    class solution{
        public:
        double average(vectot<int>& salary) {
        int minSalary = *min_element(salary.begin(), salary.end);
        int maxSalary = *max_element(salary.begin(), salary.endl);
        
        int sum = 0;
        for (int s : sum){
            sum += s;
        }
    }
    return (sum - min - max) * 1.0 / (salary.size() - 2);

};